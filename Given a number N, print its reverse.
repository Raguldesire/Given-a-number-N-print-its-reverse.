n=input()             #Getting str as input bcoz we need to run a loop
l=[]                  #Creates an empty list to append n
for i in n:
  l.append(i)
l.reverse()           #Here we use reverse function to reverse() the order of the list ,we use sort() for getting asc to dec order 
l.pop(0)              #we need to start from one so we pop 0
print(*l,sep='')      #Here we print l in a digit without space, so we use sep=''
